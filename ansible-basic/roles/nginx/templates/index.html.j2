<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{ web_message }}</title>
  <style>
    body { font-family: sans-serif; text-align: center; margin-top: 8%; background: #f4f6f8; }
    img  { max-width: 480px; width: 90%; margin-top: 2rem; }
  </style>
</head>
<body>
  <h1>{{ web_message }}</h1>
  <img src="/{{ image_file }}" alt="Image from PostgreSQL">
  <p><small>Image stored in PostgreSQL on {{ groups['dbservers'][0] }}</small></p>
</body>
</html>

